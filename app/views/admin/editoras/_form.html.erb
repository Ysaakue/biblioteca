<%= simple_form_for(@editora) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome %>
    <%= f.association :estado %>
    <%= f.association :cidade %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
