<div class="page-header">
  <h1>Editoras</h1>
</div>

<%= render partial: "search_form" %>

<div class="row">
  <div class="col-lg-12">
    <div class="ibox float-e-margins">
      <div class="ibox-content">
        <div class="table-responsive">
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Nome</th>
                <th>Cidade</th>
                <th>Ações</th>
              </tr>
            </thead>
            <tbody>
              <% @editoras.each do |editora| %>
                <tr>
                  <td><%= editora.nome %></td>
                  <td><%= editora.cidade %></td>
                  <td>
                    <%= link_to 'Exibir', admin_editora_path(editora), class: 'btn btn-xs btn-default' %>
                    <%= link_to 'Editar', edit_admin_editora_path(editora), class: 'btn btn-xs btn-default' %>
                    <%= link_to 'Deletar', admin_editora_path(editora), method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn btn-xs btn-danger' %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
          <%= paginate @editoras %>
        </div>
      </div>
    </div>
  </div>
</div>