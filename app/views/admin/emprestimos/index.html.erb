
<div class="page-header">
  <h1>Emprestimos</h1>
</div>

<%= render partial: "search_form" %>

<div class="row">
  <div class="col-lg-12">
    <div class="ibox float-e-margins">
      <div class="ibox-content">
        <div class="table-responsive">
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Aluno</th>
                <th>Bibliotecario</th>
                <th>Dt. prev. Devolução</th>
                <th>Ações</th>
              </tr>
            </thead>
            <tbody>
              <% @emprestimos.each do |emprestimo| %>
                <tr>
                  <td><%= User.find(emprestimo.aluno_id).nome %></td>
                  <td><%= User.find(emprestimo.bibliotecario_id).nome %></td>
                  
                  <td><%= emprestimo.data_prev_dev %></td>
                  <td>
                    <%= link_to 'Exibir', admin_emprestimo_path(emprestimo), class: 'btn btn-xs btn-default' %>
                    <%= link_to 'Editar', edit_admin_emprestimo_path(emprestimo), class: 'btn btn-xs btn-default' %>
                    <%= link_to 'Devolver', new_admin_devolucao_path(emprestimo_id: emprestimo.id), class: 'btn btn-xs btn-primary' %>
                    <%= link_to 'Deletar', admin_emprestimo_path(emprestimo), method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn btn-xs btn-danger' %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
          <%= paginate @emprestimos %>
        </div>
      </div>
    </div>
  </div>
</div>
