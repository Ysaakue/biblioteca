<%= simple_form_for(@reserva) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :livro %>
    <%= f.input :retirado %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
